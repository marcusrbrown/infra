---
"@marcusrbrown/infra": patch
---

Gateway deploy now forwards operator auth/config environment values (`GATEWAY_OPERATOR_GITHUB_CLIENT_ID`, `GATEWAY_OPERATOR_GITHUB_CLIENT_SECRET`, `GATEWAY_OPERATOR_CSRF_SECRET`, `GATEWAY_OPERATOR_ALLOWLIST`) required for the Gateway operator web surface.
