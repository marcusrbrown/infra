---
name: Update Repo Settings
run-name: Update Repo Settings

on:
  push:
    branches: [main]
  schedule:
    - cron: '02 18 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update-repo-settings:
    name: Update Repo Settings
    secrets:
      APPLICATION_ID: ${{ secrets.APPLICATION_ID }}
      APPLICATION_PRIVATE_KEY: ${{ secrets.APPLICATION_PRIVATE_KEY }}
    uses: bfra-me/.github/.github/workflows/update-repo-settings.yaml@dd02bc5fb90edbe510033b02657ca0e9c32af58f # v4.16.44
