---
name: Update Repo Settings
run-name: Update Repo Settings

on:
  push:
    branches: [main]
  schedule:
    - cron: '02 18 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update-repo-settings:
    name: Update Repo Settings
    secrets:
      APPLICATION_ID: ${{ secrets.APPLICATION_ID }}
      APPLICATION_PRIVATE_KEY: ${{ secrets.APPLICATION_PRIVATE_KEY }}
    uses: bfra-me/.github/.github/workflows/update-repo-settings.yaml@8aba87302817a5b675c08d32e32443bfab7fc224 # v4.19.0
