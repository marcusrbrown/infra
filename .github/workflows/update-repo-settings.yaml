---
name: Update Repo Settings
run-name: Update Repo Settings

on:
  push:
    branches: [main]
  schedule:
    - cron: '02 18 * * *'
  workflow_dispatch:

jobs:
  update-repo-settings:
    name: Update Repo Settings
    secrets: inherit
    uses: bfra-me/.github/.github/workflows/update-repo-settings.yaml@5fcdb1b914106f454f9b703fe1b28026fe4aa2a1 # v4.7.0
