# OpenCode slash commands

Markdown slash commands for OpenCode workflows. Generated documentation
(ARCHITECTURE.md, STRUCTURE.md, READMEs) is owned by the
`generating-project-docs` skill, not a slash command.
